{
  "name": "merge",
  "version": "1.0.0",
  "description": "对象合并",
  "main": "dist/merge.js",
  "browser": "dist/merge.umd.js",
  "module": "dist/merge.es.js",
  "jsnext:main": "dist/merge.es.js",
  "repository": "https://github.com/blackcater/merge.git",
  "author": "blackcater <blackcater2015@gmail.com>",
  "license": "MIT",
  "private": false,
  "scripts": {
    "dev": "rollup -c rollup.config.js -w",
    "pro": "rollup -c rollup.config.js",
    "lint": "eslint --fix '{src,test}/**/*.js'",
    "prettier": "prettier --write '{src,test}/**/*.js'",
    "test": "jest -c jest.config.json",
    "test:watch": "jest -c jest.config.json --watch",
    "test:coverage": "jest -c jest.config.json --coverage",
    "coveralls": "cat ./coverage/lcov.info | coveralls",
    "precommit": "lint-staged"
  },
  "devDependencies": {
    "babel-core": "^6.26.0",
    "babel-jest": "^22.4.1",
    "babel-preset-env": "^1.6.1",
    "coveralls": "^3.0.0",
    "eslint": "^4.18.1",
    "eslint-config-uber-es2015": "^3.1.2",
    "husky": "^0.14.3",
    "jest": "^22.4.2",
    "lint-staged": "^7.0.0",
    "prettier": "^1.10.2",
    "rollup": "^0.56.3",
    "rollup-plugin-babel": "^3.0.3"
  }
}
